[project]
name = "journal-models"
version = "0.1.0"
description = "Canonical journal metadata schema models"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.0",
    "python-dotenv>=1.2.2",
]
