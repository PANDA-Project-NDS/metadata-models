[project]
name = "golden-pipeline"
version = "0.1.0"
description = "Golden metadata generation pipeline"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.0",
    "pydantic-ai>=1.0",
    "llama-index>=0.10.0",
    "python-dotenv>=1.0",
]
